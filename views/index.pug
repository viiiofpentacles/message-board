extends layout

block content
  h1= title
  a(href='/new') Add a new message!
  each message in messages
    div(class='message-card')
      div= `From: ${message.user}`
      div= message.added 
      div= message.text
