extends layout

block content
  h1= title
  a(href='/new') Add a new message!
  div(class='message-container')
    each message in messages
      div(class='message-card')
        div= `From: ${message.user}`
        div(class='message-time')= message.added 
        div(class='message-text')= message.text
